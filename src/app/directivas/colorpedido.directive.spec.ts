import { ColorpedidoDirective } from './colorpedido.directive';

describe('ColorpedidoDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorpedidoDirective();
    expect(directive).toBeTruthy();
  });
});
