import { ColormesaDirective } from './colormesa.directive';

describe('ColormesaDirective', () => {
  it('should create an instance', () => {
    const directive = new ColormesaDirective();
    expect(directive).toBeTruthy();
  });
});
